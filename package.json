{
  "name": "typingpro-monorepo",
  "private": true,
  "version": "1.0.71",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@typingpro/desktop",
    "build": "npm run build --workspace=@typingpro/desktop",
    "tauri": "npm run tauri --workspace=@typingpro/desktop",
    "release:patch": "npm version patch --no-git-tag-version && node scripts/sync-tauri-version.cjs"
  },
  "devDependencies": {
    "@types/react": "^19.2.7",
    "@types/react-dom": "^19.2.3",
    "eslint": "^8.0.0",
    "prettier": "^3.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@tauri-apps/plugin-process": "^2.3.1",
    "@tauri-apps/plugin-store": "^2.4.1",
    "@tauri-apps/plugin-updater": "^2.9.0",
    "use-sound": "^5.0.0"
  }
}
